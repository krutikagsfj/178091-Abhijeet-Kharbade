export class Games
{
    id? : number;
    gameName?: string;
    gamePrice?: string;
                                                                                                                                                                                                                                                                                                        
}

// model using because angular needs to know what kind of data is in backend to show it in front end